[program:{{ pgsql_name }}]
command={{ pgsql_folder }}/bin/postgres --config-file={{ pgsql_folder }}/etc/postgresql.conf
socket=tcp://{{ loopback_adress }}:{{ pgsql_port }}
autostart=true
autorestart=true
startsecs=5
stopwaitsecs=5
stopasgroup=true
killasgroup=true
